# line break as `n
echo "foon`bar`nv1`nv2`n"

# or use >> to more than one line, each end line must has `
echo "foo`
bar`
v1`
v2"

# if input will show >> like below, to notice input line
# echo "foo`
# >> bar`
# >> v1`
# >> v2"